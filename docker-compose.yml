version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: hospital_assets_postgres
    restart: always
    environment:
      POSTGRES_DB: hospital_assets
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
